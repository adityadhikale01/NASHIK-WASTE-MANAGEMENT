<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login page</title>
    <%- include('includes/headerLink.ejs') %>
</head>
<body>
    <div id="login" class="loginbox">
      
  <div class="container d-flex justify-content-center align-items-center min-vh-100">
    <div class="card shadow p-4" style="max-width: 400px; width: 100%;">
      <div class="card-body">
        <h3 class="card-title text-center mb-4 text-success">Waste Management Login</h3>
        <form action="/login" method="POST">
          <div class="mb-3">
            <label for="mobileno" class="form-label">MOBILE NO:</label>
            <input type="tel" pattern="[0-9]{10}" maxlength="10" name="mobileno" class="form-control" id="mobileno" placeholder="+91" title="Please enter valid number" required>
            <small class="form-text text-muted">Enter Adhaar Linked mobile number</small>
          </div>

          <div class="mb-3">
            <label for="password" class="form-label">Password</label>
            <input type="password" name="pass" class="form-control" id="password" placeholder="********" required>
          </div>

          <div class="d-grid">
            <button type="submit" class="btn btn-success">Login</button>
          </div>
        </form>

        <p class="mt-3 text-center">
          Don't have an account?
          <a href="/signup" class="text-decoration-none text-success">Signup here</a>
        </p>
      </div>
    </div>
  </div>
    </div>

</body>
</html>