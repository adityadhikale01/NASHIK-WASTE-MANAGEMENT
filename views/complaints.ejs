<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Complaints</title>
     <link rel="stylesheet" href="general.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" crossorigin="anonymous" 
  
  referrerpolicy="no-referrer" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com"   crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Playwrite+AU+QLD:wght@100..400&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="icon" href="./image/Waste_Management_logo.svg">
  <!-- complian pg -->
   <title>Smart Waste Complaint Form</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    :root {
      --green: #28a745;
      --light-green: #e6f9ec;
      --dark-green: #155724;
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      padding: 0;
      font-family: 'Inter', sans-serif;
      background: linear-gradient(to bottom right, #eafaf1, #c8f5d9);
      color: #333;
    }

    .pg-header {
      text-align: center;
      padding: 40px 20px 20px;
    }

    .pg-header img {
      max-width: 180px;
      margin-bottom: 20px;
    }

    .pg-header h1 {
      font-size: 2rem;
      color: var(--dark-green);
    }

    .pg-header p {
      color: #444;
      font-size: 1rem;
      margin-top: 10px;
    }

    .container {
      max-width: 700px;
      margin: 0 auto 60px;
      background: rgba(255, 255, 255, 0.8);
      padding: 30px;
      border-radius: 16px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
      backdrop-filter: blur(10px);
    }

    label {
      display: block;
      margin-bottom: 8px;
      font-weight: 600;
      color: #2e7d32;
    }

    input, select, textarea {
      width: 100%;
      padding: 12px;
      border: 1px solid #c3e6cb;
      border-radius: 8px;
      font-size: 1rem;
      margin-bottom: 20px;
      background-color: #fff;
    }

    textarea {
      resize: vertical;
      min-height: 100px;
    }

    input[type="file"] {
      background-color: #f0fff4;
    }

    #preview {
      display: none;
      max-width: 100%;
      margin-top: -10px;
      margin-bottom: 20px;
      border-radius: 10px;
      border: 1px solid #ccc;
      transition: all 0.3s ease;
    }

    button {
      background-color: var(--green);
      color: white;
      border: none;
      padding: 14px;
      font-size: 1rem;
      border-radius: 8px;
      width: 100%;
      cursor: pointer;
      transition: background 0.3s ease;
    }

    button:hover {
      background-color: #218838;
    }

    .additional {
      margin-top: 50px;
    }

    .additional h2 {
      color: var(--dark-green);
      font-size: 1.3rem;
      margin-bottom: 20px;
    }

    .complaint-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
      gap: 20px;
    }

    .complaint-item {
      background: var(--light-green);
      padding: 15px;
      border-radius: 12px;
      text-align: center;
      transition: transform 0.3s;
    }

    .complaint-item:hover {
      transform: translateY(-5px);
    }

    .complaint-item img {
      width: 40px;
      margin-bottom: 10px;
    }

    .complaint-item p {
      font-weight: 600;
      color: #1b5e20;
    }

    .toast {
      display: none;
      position: fixed;
      bottom: 30px;
      right: 30px;
      background: #28a745;
      color: white;
      padding: 15px 20px;
      border-radius: 10px;
      box-shadow: 0 0 15px rgba(0,0,0,0.1);
      animation: fadeInOut 3s ease;
    }

    @keyframes fadeInOut {
      0% { opacity: 0; transform: translateY(20px); }
      10% { opacity: 1; transform: translateY(0); }
      90% { opacity: 1; }
      100% { opacity: 0; transform: translateY(20px); }
    }

    @media (max-width: 600px) {
      header h1 {
        font-size: 1.5rem;
      }
    }
  </style>
</head>
<body>
  <!-- section1 -->
<div class="section1">
<header>
    <div class="logo"></div>
    <div class="slogan">
         <h1 >Smart City Initiative</h1>
         <p>:Together For Cleaner City</p>
    </div>
  
  </header>
<div class="sub_header"   style="top: 73px;" >
   
    <nav>
      <ul>
        <li><a href="#type">Types of Complaints</a></li>
        <li><a href="#app">Digital App</a></li>
        <li><a href="#contact">Contact</a></li>
      </ul>
    </nav>
  </div>

<div class="empty-box"></div>
<div class="pg-header">
    
    <h1>Make Complaint Form</h1>
    <p>Help us keep your area clean by reporting waste issues with a geotagged photo.</p>
</div> 

  <div class="container">
    <form id="complaintForm">
      <label for="name">Your Name</label>
      <input type="text" id="name" name="name" placeholder="John Doe">

      <label for="email">Email</label>
      <input type="email" id="email" name="email" placeholder="you@example.com">

      <label for="location">Location</label>
      <input type="text" id="location" name="location" placeholder="E.g. Sector 21, Main Road">

      <label for="type">Complaint Type</label>
      <select id="type" name="type">
        <option value="">Select a Complaint Type</option>
        <option>Overflowing Waste Bins</option>
        <option>Missed Collection</option>
        <option>Burning of Waste</option>
        <option>Rodents/Insects</option>
        <option>Other</option>
      </select>

      <label for="photo">Upload Geotagged Photo</label>
      <input type="file" id="photo" name="photo" accept="image/*" onchange="previewImage(event)">
      <img id="preview" alt="Image preview" />

      <label for="description">Additional Details</label>
      <textarea id="description" name="description" placeholder="Write more about the issue if needed..."></textarea>

      <button type="submit">Submit Complaint</button>
    </form>

    <div class="additional">
      <h2>Additional Complaints</h2>
      <div class="complaint-grid">
        <div class="complaint-item">
          <img src="https://img.icons8.com/ios-filled/50/trash.png" alt="Overflowing Bins">
          <p>Overflowing Bins</p>
        </div>
        <div class="complaint-item">
          <img src="https://img.icons8.com/ios-filled/50/fire-element.png" alt="Burning of Waste">
          <p>Burning of Waste</p>
        </div>
        <div class="complaint-item">
          <img src="https://img.icons8.com/ios-filled/50/delete-sign.png" alt="Missed Pickup">
          <p>Missed Pickup</p>
        </div>
        <div class="complaint-item">
          <img src="https://img.icons8.com/ios-filled/50/recycle.png" alt="Circular Economy">
          <p>Circular Economy</p>
        </div>
        <div class="complaint-item">
          <img src="https://img.icons8.com/ios-filled/50/plant-under-sun.png" alt="Environmental Benefits">
          <p>Environmental Benefits</p>
        </div>
      </div>
    </div>
  </div>

  <div class="toast" id="toast">Complaint submitted successfully!</div>

  
  <script>
    function previewImage(event) {
      const file = event.target.files[0];
      const preview = document.getElementById('preview');

      if (file && file.type.startsWith('image/')) {
        const reader = new FileReader();
        reader.onload = function (e) {
          preview.src = e.target.result;
          preview.style.display = 'block';
        };
        reader.readAsDataURL(file);
      } else {
        preview.src = '';
        preview.style.display = 'none';
        alert("Please upload a valid image file.");
      }
    }

    document.getElementById('complaintForm').addEventListener('submit', function(e) {
      e.preventDefault();

      const location = document.getElementById('location').value.trim();
      const type = document.getElementById('type').value.trim();

      if (!location || !type) {
        alert("Please fill in at least the location and complaint type.");
        return;
      }

     // Hide toast after 3 seconds
      setTimeout(() => {
        toast.style.display = 'none';
        document.getElementById('complaintForm').reset();
        document.getElementById('preview').style.display = 'none';
      }, 3000);

      // You can replace this with an actual API call to your backend/server
      console.log("Complaint submitted!");
    });
  </script>
</div>

<!-- section2 -->   
<%- include('includes/section2.ejs') %>
      
</body>
</html>